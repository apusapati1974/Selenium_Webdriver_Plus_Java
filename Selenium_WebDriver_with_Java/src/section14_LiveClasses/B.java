package section14_LiveClasses;

public class B {
	public static void main(String[] args) {
		
		Interface_A.method1();
		/* We can able to call  static method of interface directly by the interface name.
		 * In Java 8 we can create an static method which is quite similar to concrete method and we create inside the interface.*/
		/* In Java 8, User can able to write concrete method in the interface as well and it is not simple public method and it is a static method.*/
		
	}
}
